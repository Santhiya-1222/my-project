<%- include("partials/layout", { title: "Menu", body: `

<div class="container mt-4">
  <h2 class="mb-3">Menu</h2>

  <!-- Afternoon -->

  <h4 class="mt-4">Afternoon</h4>
  <div class="row">
    ${afternoon.map(item => `
      <div class="col-md-4 mb-3">
        <div class="card p-3 shadow-eatclub">

```
      <h5>${item.name}</h5>
      <p>₹${Number(item.price).toFixed(0)}</p>

      <form method="POST" action="/cart/add">
        <input type="hidden" name="name" value="${item.name}">
        <input type="hidden" name="price" value="${item.price}">
        
        <button type="submit" class="btn btn-eatclub w-100">
          Add to Cart
        </button>
      </form>

    </div>
  </div>
`).join("")}
```

  </div>

  <!-- Evening -->

  <h4 class="mt-4">Evening</h4>
  <div class="row">
    ${evening.map(item => `
      <div class="col-md-4 mb-3">
        <div class="card p-3 shadow-eatclub">

```
      <h5>${item.name}</h5>
      <p>₹${Number(item.price).toFixed(0)}</p>

      <form method="POST" action="/cart/add">
        <input type="hidden" name="name" value="${item.name}">
        <input type="hidden" name="price" value="${item.price}">
        
        <button type="submit" class="btn btn-eatclub w-100">
          Add to Cart
        </button>
      </form>

    </div>
  </div>
`).join("")}
```

  </div>

</div>

` }) %>
